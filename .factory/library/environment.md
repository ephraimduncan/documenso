# Environment

**What belongs here:** Required env vars, external dependencies, setup notes.
**What does NOT belong here:** Service ports/commands (use `.factory/services.yaml`).

---

- Node >= 22, npm >= 10.7.0
- Postgres on localhost:5432 (managed externally)
- `.env` file exists at repo root (copied from `.env.example`)
- No Docker needed for this mission
